{
  "title": "graphql-api",
  "description": "Design and implement GraphQL APIs with proper schema design, resolvers, and best practices.",
  "trigger": "When you need flexible APIs with precise data requirements or nested data fetching",
  "input": "GraphQL schema definition, resolvers, data sources",
  "steps": [
    "Define GraphQL schema with types, queries, mutations",
    "Implement resolver functions for each field",
    "Set up query optimization with DataLoader",
    "Add input validation and error handling",
    "Document with GraphQL Playground or GraphiQL"
  ],
  "output": "Functional GraphQL API with schema and resolvers",
  "use_cases": [
    "Building flexible APIs for mobile apps",
    "Aggregating data from multiple sources",
    "API with complex nested queries"
  ],
  "limitations": "Overhead for simple CRUD APIs; caching challenges"
}
