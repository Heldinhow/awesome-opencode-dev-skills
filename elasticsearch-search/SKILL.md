{
  "title": "elasticsearch-search",
  "description": "Implement full-text search capabilities using Elasticsearch for scalable search solutions.",
  "trigger": "When you need powerful full-text search with ranking and filtering",
  "input": "Search requirements, document mappings, query specifications",
  "steps": [
    "Set up Elasticsearch cluster and index",
    "Define document mappings with appropriate analyzers",
    "Index documents with proper field configurations",
    "Build search queries (match, term, bool, etc.)",
    "Implement aggregations for faceted search"
  ],
  "output": "Full-text search functionality with relevance ranking",
  "use_cases": [
    "Building search engines",
    "Implementing faceted product search",
    "Log and analytics aggregation"
  ],
  "limitations": "Resource-intensive for large datasets; requires separate infrastructure"
}
