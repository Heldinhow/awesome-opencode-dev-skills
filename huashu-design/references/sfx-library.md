# SFX Library · huashu-design

> All generated with the ElevenLabs Sound Generation API, at Apple-keynote-grade sound quality.
> A production-grade SFX asset library covering the full range of Huashu animations, presentations, and product demos.

**Asset location**: `assets/sfx/<category>/<name>.mp3`
**Total**: 37 SFX (30 batch-generated + 7 retained from v7b)
**Generation model**: ElevenLabs Sound Generation API (`prompt_influence 0.4`)
**Sound quality**: 44.1kHz MP3, Apple-keynote-grade clarity, no extra reverb

---

## Directory Structure

```
assets/sfx/
├── keyboard/      type, type-fast, delete-key, space-tap, enter
├── ui/            click, click-soft, focus, hover-subtle, tap-finger, toggle-on
├── transition/    whoosh, whoosh-fast, swipe-horizontal, slide-in, dissolve
├── container/     card-snap, card-flip, stack-collapse, modal-open
├── feedback/      success-chime, error-tone, notification-pop, achievement
├── progress/      loading-tick, complete-done, generate-start
├── impact/        logo-reveal, logo-reveal-v2, brand-stamp, drop-thud
├── magic/         sparkle, ai-process, transform
└── terminal/      command-execute, output-appear, cursor-blink
```

---

## Quick Index

### ⌨️ Keyboard

| File | Duration | Use | Prompt highlights |
|---|---|---|---|
| `sfx/keyboard/type.mp3` | 0.5s | Single key press (mechanical keyboard single key) | mechanical keyboard single key press |
| `sfx/keyboard/type-fast.mp3` | 1.5s | Continuous fast typing (prompt input demo) | fast continuous typing rhythm, apple magic keyboard |
| `sfx/keyboard/delete-key.mp3` | 0.5s | Backspace delete | single backspace key, low pitched thud |
| `sfx/keyboard/space-tap.mp3` | 0.5s | Soft spacebar tap | soft spacebar tap, wide flat |
| `sfx/keyboard/enter.mp3` | 0.5s | Enter confirm (retained from v7b) | enter key press, crisp tactile |

### 🎯 UI

| File | Duration | Use | Prompt highlights |
|---|---|---|---|
| `sfx/ui/click.mp3` | 0.5s | Standard UI click (retained from v7b) | crisp modern interface click |
| `sfx/ui/click-soft.mp3` | 0.5s | Softer UI click (secondary button/link) | soft gentle button click, mid pitched |
| `sfx/ui/focus.mp3` | 0.5s | Element focus/selection (retained from v7b) | subtle focus tone, element highlight |
| `sfx/ui/hover-subtle.mp3` | 0.5s | Hover hint (micro-feedback) | barely audible tick, air whisper |
| `sfx/ui/tap-finger.mp3` | 0.5s | Mobile tap (iOS UI) | finger tap on touchscreen, muted thud |
| `sfx/ui/toggle-on.mp3` | 0.5s | Toggle on | ios toggle switch flip, satisfying click |

### 🌊 Transition

| File | Duration | Use | Prompt highlights |
|---|---|---|---|
| `sfx/transition/whoosh.mp3` | 0.5s | Standard whoosh (retained from v7b) | air whoosh transition |
| `sfx/transition/whoosh-fast.mp3` | 0.6s | Fast whoosh (title flash-in, tab switch) | quick fast air whoosh, cinematic |
| `sfx/transition/swipe-horizontal.mp3` | 0.7s | Horizontal swipe (carousel, tab switch) | smooth left-to-right air movement |
| `sfx/transition/slide-in.mp3` | 0.6s | Element slide-in (side panel, drawer) | smooth soft whoosh with arrival |
| `sfx/transition/dissolve.mp3` | 0.8s | Soft dissolve (image fade through) | soft dissolve, airy shimmer |

### 🃏 Container

| File | Duration | Use | Prompt highlights |
|---|---|---|---|
| `sfx/container/card-snap.mp3` | 0.5s | Card snap / settle into place (retained from v7b) | card snap into place |
| `sfx/container/card-flip.mp3` | 0.7s | Card flip (front/back switch) | playing card flip, crisp snap |
| `sfx/container/stack-collapse.mp3` | 0.8s | Stack collapsing inward (list aggregation) | cards stacking, paper taps collapsing |
| `sfx/container/modal-open.mp3` | 0.6s | Modal opening | modal popping open, whoosh + thud |

### 🔔 Feedback

| File | Duration | Use | Prompt highlights |
|---|---|---|---|
| `sfx/feedback/success-chime.mp3` | 1.0s | Success cue (payment complete, task done) | two ascending bell tones, ios-style |
| `sfx/feedback/error-tone.mp3` | 0.7s | Error cue (warning, failure) | descending two-note warning, soft |
| `sfx/feedback/notification-pop.mp3` | 0.6s | Message pop-up (toast, notification) | notification bloop, ios message alert |
| `sfx/feedback/achievement.mp3` | 1.5s | Achievement/milestone/badge | triumphant rising arpeggio, game-style |

### ⏳ Progress

| File | Duration | Use | Prompt highlights |
|---|---|---|---|
| `sfx/progress/loading-tick.mp3` | 0.5s | Loading pulse (progress bar beat) | soft short pulse, minimal ambient |
| `sfx/progress/complete-done.mp3` | 0.8s | Completion confirm (step complete) | two ascending satisfying tones |
| `sfx/progress/generate-start.mp3` | 0.8s | AI generation start | soft rising shimmer, magical whoosh |

### 💥 Impact

| File | Duration | Use | Prompt highlights |
|---|---|---|---|
| `sfx/impact/logo-reveal.mp3` | 0.7s | Logo impact (retained from v7b) | logo reveal thud |
| `sfx/impact/logo-reveal-v2.mp3` | 1.5s | Longer logo impact (more cinematic) | cinematic bass hit with shimmer tail |
| `sfx/impact/brand-stamp.mp3` | 1.0s | Stamp-like hit (certification, seal) | rubber stamp thud, paper contact |
| `sfx/impact/drop-thud.mp3` | 0.7s | Object landing (insert, place down) | heavy thud, wood surface contact |

### ✨ Magic

| File | Duration | Use | Prompt highlights |
|---|---|---|---|
| `sfx/magic/sparkle.mp3` | 0.8s | Magic sparkle (AI highlight, surprise) | bright twinkling stars, fairy dust |
| `sfx/magic/ai-process.mp3` | 1.2s | AI processing sound (thinking state) | modulating digital hum with shimmer |
| `sfx/magic/transform.mp3` | 1.0s | Transformation transition (morph effect) | rising shimmer whoosh with sparkle tail |

### 💻 Terminal

| File | Duration | Use | Prompt highlights |
|---|---|---|---|
| `sfx/terminal/command-execute.mp3` | 0.5s | Command execution | crisp digital beep with tick, hacker ui |
| `sfx/terminal/output-appear.mp3` | 0.6s | Output appearing | rapid digital ticks, retro printout |
| `sfx/terminal/cursor-blink.mp3` | 0.5s | Cursor blink | subtle soft digital pulse, rhythmic |

---

## Recommended Pairings by Scenario

### 💻 Terminal interaction demo
```
type (0.5s) → enter (0.5s) → command-execute (0.5s) → output-appear (0.6s)
```
Looping element: `cursor-blink` as an idle ambient layer.

### 🃏 Card selection flow
```
hover-subtle (0.5s, UI hover) → click-soft (0.5s, click) → card-snap (0.5s, settle)
```
Or the advanced version: use `card-flip` for front/back switching.

### 🤖 Full AI generation flow
```
generate-start (0.8s, start) → ai-process (1.2s, processing) → sparkle (0.8s, flash) → complete-done (0.8s, done)
```
Use `error-tone` instead of `complete-done` on error.

### 🎬 Logo Reveal (brand moment)
```
whoosh-fast (0.6s, setup) → logo-reveal-v2 (1.5s, landing) → sparkle (0.8s, tail)
```
Simple version: `whoosh → logo-reveal` (the direct v7b two-piece combo).

### 📱 UI interaction demo (mobile)
```
tap-finger (0.5s, tap) → slide-in (0.6s, panel slide-in) → toggle-on (0.5s, switch)
```
After completion: `success-chime` or `notification-pop`.

### 📊 Data visualization / dashboard
```
loading-tick (0.5s, beat) × N → complete-done (0.8s, data arrives) → achievement (1.5s, impressive landing)
```

### 🎯 Form submission flow
```
click-soft (0.5s) → loading-tick ×2 (1.0s) → success-chime (1.0s)
```
Failure branch: `error-tone (0.7s)`.

### 🪄 Magic Transform scene
```
whoosh-fast (0.6s) → transform (1.0s) → sparkle (0.8s)
```
Good for element morphs, before/after effect comparisons, "AI rewrite" demos, etc.

---

## Usage Rules

### Volume recommendations (from the two-track audio system in `apple-gallery-showcase.md`)
- **SFX main track**: `1.0` (no attenuation)
- **BGM background track**: `0.4 ~ 0.5` (SFX should clearly cut through)
- **Multiple SFX mixed together**: use `amix=inputs=N:duration=longest:normalize=0` to preserve dynamic range

### ffmpeg stitching templates
```bash
# Align a single SFX to a specific timestamp:
ffmpeg -i video.mp4 -itsoffset 2.5 -i sfx/ui/click.mp3 \
  -filter_complex "[0:a][1:a]amix=inputs=2:duration=longest:normalize=0[a]" \
  -map 0:v -map "[a]" output.mp4

# Multiple SFX + BGM:
ffmpeg -i video.mp4 \
  -itsoffset 1.0 -i sfx/transition/whoosh-fast.mp3 \
  -itsoffset 1.6 -i sfx/impact/logo-reveal-v2.mp3 \
  -i bgm.mp3 \
  -filter_complex "[3:a]volume=0.4[bgm];[0:a][1:a][2:a][bgm]amix=inputs=4:normalize=0[a]" \
  -map 0:v -map "[a]" output.mp4
```

### Selection decision tree
1. **Tactile action present** (typing/clicking/sliding) → `keyboard/` or `ui/`
2. **Element entering/leaving** → `transition/`
3. **Container-level action** (card/modal) → `container/`
4. **State feedback** (success/failure/notification) → `feedback/`
5. **Progress/time passing** → `progress/`
6. **Brand landing/important moment** → `impact/`
7. **AI magic/transformation** → `magic/`
8. **Terminal/code demo** → `terminal/`

### Avoid stacking too many sounds
- At the same timestamp, allow **at most 2 concurrent SFX**
- If BGM is below 0.3, you can allow 3
- For a brand impact moment, clear other SFX first (leave 0.2s of air, then land the hit)

---

## Prompt Writing Principles (for reuse)

Reference style: `apple keynote, tight, minimal, no reverb unless ambient, crisp, elegant`

**The three ingredients of a good prompt**:
1. **Physical sound description**: what object, what action (`"mechanical keyboard single key press"`)
2. **Texture/style constraint**: apple-style / ios-style / cinematic / retro
3. **Negative constraints**: no reverb / clean studio / minimal

❌ `"click sound"`
✅ `"crisp ui button click, clean modern interface sound, apple-style, high pitched"`

❌ `"magic"`
✅ `"bright twinkling stars sound, high pitched glittery chime, fairy dust"`

---

## See Also
- Two-track audio and ffmpeg stitching: `apple-gallery-showcase.md`
- Original generation script: `/tmp/gen_sfx_batch.sh` (one-off batch generator)
